/**
 * Flujo del bowl — variante VIS (Vivienda de Interés Social).
 *
 * Pieza del sistema: FRONTEND (Carlos).
 *
 * Define el orden y los pasos del bowl para proyectos VIS. La diferencia con
 * la variante No VIS nace del hallazgo del sitio real (subsidios, topes de
 * ingreso, preguntas de afiliación distintas).
 *
 * TODOs:
 *  - [ ] Listar los pasos VIS (cada uno -> un campo del contrato, sección 4).
 *  - [ ] Marcar campos obligatorios vs. opcionales para VIS.
 *  - [ ] Ajustar los rangos de ingreso relevantes para VIS.
 */

// TODO(Carlos): definir la secuencia de pasos VIS.
export const flujoVis: { pasos: string[] } = {
  pasos: [], // TODO: p.ej. ["afiliacion", "ingreso", "zona", "urgencia", ...]
};
