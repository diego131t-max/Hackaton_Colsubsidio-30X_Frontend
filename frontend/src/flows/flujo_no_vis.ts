/**
 * Flujo del bowl — variante No VIS (ej. Nuva Park).
 *
 * Pieza del sistema: FRONTEND (Carlos).
 *
 * Define el orden y los pasos del bowl para proyectos No VIS. Sin los topes ni
 * las preguntas de subsidio propias de VIS.
 *
 * TODOs:
 *  - [ ] Listar los pasos No VIS (cada uno -> un campo del contrato, sección 4).
 *  - [ ] Ajustar bandas de precio/ingreso para No VIS.
 *  - [ ] Confirmar qué proyectos usan este flujo (ej. Nuva Park).
 */

// TODO(Carlos): definir la secuencia de pasos No VIS.
export const flujoNoVis: { pasos: string[] } = {
  pasos: [], // TODO
};
