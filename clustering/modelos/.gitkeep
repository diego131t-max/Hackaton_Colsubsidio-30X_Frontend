# Carpeta de modelos serializados de clustering (Santiago DS).
# El .pkl real (ej. kmeans_v1.pkl) NO se versiona directamente si es pesado:
# ver .gitignore raíz (ignora *.pkl) y usar git-lfs si hace falta versionarlo.
