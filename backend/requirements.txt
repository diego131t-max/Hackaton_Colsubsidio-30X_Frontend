# Backend — Reto Vivienda Colsubsidio x 30X
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27          # POST real al webhook del flow de Dapta
python-dotenv>=1.0   # carga .env (URL/secretos) en desarrollo local

# Motor de recomendaciones v2 (Nearest Neighbors sobre el historial).
# Version minima de numpy alineada con la que pide scikit-learn 1.3+.
numpy>=1.24
scikit-learn>=1.3
